import 'package:flutter/material.dart';

class Deportes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Pantalla de deportes"),
    );
  }
}

