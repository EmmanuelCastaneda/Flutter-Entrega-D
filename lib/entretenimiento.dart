import 'package:flutter/material.dart';

class Entretenimiento extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Pantalla de Entretenimiento"),
    );
  } 
}